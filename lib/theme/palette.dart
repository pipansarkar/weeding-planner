import 'package:flutter/material.dart';

/// The app's six "at a glance" stat colors, tuned to sit together as a
/// single muted, professional family instead of a rainbow of saturated
/// Material primaries.
const List<Color> statPalette = [
  Color(0xFF0F766E), // teal
  Color(0xFFB45309), // amber
  Color(0xFF6D28D9), // violet
  Color(0xFF1D4ED8), // blue
  Color(0xFFBE185D), // rose
  Color(0xFF334155), // slate
];
